# Offer Calculator

Cloudflare：<https://offer-calculator.xiaosang.cc/> · GitHub Pages：<https://holynova.github.io/offer_calculator/>

本项目使用 Vite 构建：`pnpm run build` 生成 `dist/`，`pnpm run build:pages` 将同一产物同步到 `docs/`。
